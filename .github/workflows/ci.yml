name: CI/CD

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install pytest-cov

      - name: Run tests
        run: cd backend && python -m pytest tests/ -v

      - name: Generate coverage report
        run: cd backend && python -m pytest tests/ --cov=app --cov-report=xml --cov-report=term-missing

      - name: Upload coverage
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: backend/coverage.xml

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff

      - name: Lint check
        run: ruff check backend/app/

      - name: Format check
        run: ruff format --check backend/app/

  deploy:
    needs: [test, lint]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to server
        run: echo "Deploy step - configure for your server"
        # 示例部署步骤（根据实际服务器配置修改）:
        # - scp 文件到服务器
        # - ssh 到服务器执行部署命令
        # - 重启服务
        #
        # 示例:
        # - name: Copy files to server
        #   run: scp -r ./* user@server:/path/to/app/
        #
        # - name: Restart service
        #   run: ssh user@server "cd /path/to/app && docker-compose restart"
